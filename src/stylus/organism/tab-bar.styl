.tab-bar-wrapper
	overflow hidden
	.tab-bar
		margin 0
		position relative
		padding 30px 0 0 32px
		//even-blocks(32)
		min-width 100%
		height 60px
		overflow hidden
		transition transform .3s ease-in-out
		&:after
			content ''
			display block
			clear both
			float none